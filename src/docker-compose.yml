version: "3.8"

services:
  mcp_server:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./ground_truth.txt:/app/ground_truth.txt
      - ./model_output.txt:/app/model_output.txt
